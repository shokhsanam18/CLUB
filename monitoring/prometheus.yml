global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus:9090']

  - job_name: 'backend'
    metrics_path: /metrics
    scheme: https     
    static_configs:
      - targets: ['club-backend:8000']
    tls_config:
      insecure_skip_verify: true
